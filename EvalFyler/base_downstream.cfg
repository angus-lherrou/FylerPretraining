[data]

train = $$DOWNSTREAM/datasets/$experiment/train
test = $$DOWNSTREAM/datasets/$experiment/test

tokenizer_pickle = $$MODELS/$cfg/tokenizer/tokenizer.p
config_pickle = $$MODELS/$cfg/config.p
model_file = $$MODELS/$cfg/model.pt

classif_param = default

[model]

scratch = false
freeze = false
epochs = 5
batch = 8
dropout = 0.25
lr = 1e-5
