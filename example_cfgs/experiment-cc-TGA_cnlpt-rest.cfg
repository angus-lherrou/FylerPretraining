[data]
train = $DOWNSTREAM/datasets/cc-TGA/train
test = $DOWNSTREAM/datasets/cc-TGA/test
classif_param = default

[model]
scratch = false
freeze = false
epochs = 30
batch = 16
dropout = 0.1
lr = 1e-5
